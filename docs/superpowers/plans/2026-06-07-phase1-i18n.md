# Phase 1 — i18n + Language Toggle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tích hợp vue-i18n v9 vào frontend, chuyển toàn bộ UI strings sang locale keys (`vi.json` / `en.json`), thêm nút toggle VI/EN cuối sidebar — UI mặc định hiển thị tiếng Việt.

**Architecture:** Frontend-only change. `i18n.ts` tạo instance vue-i18n với locale đọc từ `localStorage('workspace.lang')` (mặc định `'vi'`). Ba component hiện tại (`SidebarNav`, `ChatPanel`, `ArtifactsPanel`) dùng `useI18n()` để lấy hàm `t()`. Backend không thay đổi.

**Tech Stack:** vue-i18n ^9.0.0, Vue 3 Composition API (`legacy: false`), localStorage, TypeScript

---

## File Map

```
frontend/
├── package.json                      [modify] — thêm vue-i18n dependency
├── src/
│   ├── i18n.ts                       [create] — createI18n instance + Locale type
│   ├── main.ts                       [modify] — app.use(i18n)
│   ├── locales/
│   │   ├── vi.json                   [create] — 21 Vietnamese keys
│   │   └── en.json                   [create] — 21 English keys
│   └── components/
│       ├── SidebarNav.vue            [modify] — useI18n, t(), VI/EN toggle button
│       ├── ChatPanel.vue             [modify] — useI18n, t(), fix toLocaleTimeString locale
│       └── ArtifactsPanel.vue        [modify] — useI18n, t()
```

---

## Task 1: Install vue-i18n + Create Locale Infrastructure

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/src/locales/vi.json`
- Create: `frontend/src/locales/en.json`
- Create: `frontend/src/i18n.ts`
- Modify: `frontend/src/main.ts`

- [ ] **Step 1: Add vue-i18n to `frontend/package.json`**

Thêm vào `dependencies` (trong `frontend/package.json`):

```json
{
  "name": "workspace-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "dompurify": "^3.0.0",
    "marked": "^9.0.0",
    "vue": "^3.3.0",
    "vue-i18n": "^9.0.0"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.0",
    "@vitejs/plugin-vue": "^4.2.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.3.0",
    "typescript": "^5.1.0",
    "vite": "^4.4.0",
    "vue-tsc": "^1.8.0"
  }
}
```

- [ ] **Step 2: Install dependency**

```bash
cd frontend
npm install
```

Expected: `node_modules/vue-i18n/` xuất hiện, không có error.

- [ ] **Step 3: Create `frontend/src/locales/vi.json`**

```json
{
  "nav.chat": "Trò chuyện",
  "nav.tasks": "Nhiệm vụ",
  "nav.files": "Tệp tin",
  "nav.settings": "Cài đặt",
  "nav.lang": "VI",
  "chat.header": "AGENT CHAT",
  "chat.mode.stub": "chế độ stub",
  "chat.placeholder": "nhập lệnh hoặc câu hỏi_",
  "chat.system.init": "Agent đã khởi động. SQLite đã kết nối. Đang ở chế độ stub.",
  "chat.user.prefix": "$ người dùng",
  "chat.agent.prefix": "▶ agent",
  "chat.system.prefix": "[hệ thống]",
  "chat.error.unreachable": "[lỗi] Không kết nối được agent. Backend đang chạy chưa?",
  "chat.loading": "…",
  "artifacts.header": "KẾT QUẢ",
  "artifacts.empty": "◈ Chưa có kết quả",
  "artifacts.label.lastReply": "phản hồi cuối",
  "health.checking": "Đang kiểm tra...",
  "health.ok": "Backend: hoạt động · DB: đã kết nối",
  "health.error": "Không kết nối được backend"
}
```

- [ ] **Step 4: Create `frontend/src/locales/en.json`**

```json
{
  "nav.chat": "Chat",
  "nav.tasks": "Tasks",
  "nav.files": "Files",
  "nav.settings": "Settings",
  "nav.lang": "EN",
  "chat.header": "AGENT CHAT",
  "chat.mode.stub": "stub mode",
  "chat.placeholder": "type a command or question_",
  "chat.system.init": "Agent initialized. SQLite connected. Stub mode active.",
  "chat.user.prefix": "$ user",
  "chat.agent.prefix": "▶ agent",
  "chat.system.prefix": "[system]",
  "chat.error.unreachable": "[error] Could not reach agent. Is the backend running?",
  "chat.loading": "…",
  "artifacts.header": "ARTIFACTS",
  "artifacts.empty": "◈ No artifacts yet",
  "artifacts.label.lastReply": "last reply",
  "health.checking": "Checking backend...",
  "health.ok": "Backend: ok · DB: connected",
  "health.error": "Backend unreachable"
}
```

- [ ] **Step 5: Create `frontend/src/i18n.ts`**

```typescript
import { createI18n } from 'vue-i18n'
import vi from './locales/vi.json'
import en from './locales/en.json'

const savedLang = localStorage.getItem('workspace.lang') ?? 'vi'

export const i18n = createI18n({
  legacy: false,
  locale: savedLang,
  fallbackLocale: 'en',
  messages: { vi, en },
})

export type Locale = 'vi' | 'en'
```

- [ ] **Step 6: Update `frontend/src/main.ts`**

```typescript
import { createApp } from 'vue'
import './assets/main.css'
import App from './App.vue'
import { i18n } from './i18n'

const app = createApp(App)
app.use(i18n)
app.mount('#app')
```

- [ ] **Step 7: Verify TypeScript compiles**

```bash
cd frontend
npm run build
```

Expected: Build thành công. Nếu có lỗi `Cannot find module './locales/vi.json'`, thêm vào `tsconfig.json`:
```json
"resolveJsonModule": true
```
(Đã có sẵn từ Phase 0 — chỉ cần check lại nếu lỗi xảy ra.)

- [ ] **Step 8: Commit**

```bash
cd ..
git add frontend/package.json frontend/package-lock.json frontend/src/i18n.ts frontend/src/locales/
git commit -m "feat(frontend): add vue-i18n v9 with Vietnamese and English locales"
```

---

## Task 2: Update SidebarNav.vue — i18n + Language Toggle

**Files:**
- Modify: `frontend/src/components/SidebarNav.vue`

- [ ] **Step 1: Replace toàn bộ nội dung `frontend/src/components/SidebarNav.vue`**

```vue
<template>
  <nav class="w-[52px] bg-cyber-dark border-r border-cyber-border flex flex-col items-center py-3 gap-2 shrink-0">
    <!-- Logo -->
    <div class="text-cyber-accent text-lg mb-2 font-mono" style="text-shadow: 0 0 8px #00d4ff">◈</div>

    <!-- Nav icons -->
    <button
      v-for="item in navItems"
      :key="item.view"
      :title="t(item.labelKey)"
      @click="$emit('navigate', item.view)"
      :class="[
        'w-9 h-9 rounded flex items-center justify-center text-base transition-colors duration-150',
        activeView === item.view
          ? 'bg-cyber-accent/10 border border-cyber-dim text-cyber-accent'
          : 'border border-transparent text-cyber-accent/40 hover:text-cyber-accent/70'
      ]"
    >
      {{ item.icon }}
    </button>

    <div class="flex-1" />

    <!-- Settings -->
    <button
      :title="t('nav.settings')"
      class="w-9 h-9 rounded flex items-center justify-center text-base border border-transparent text-cyber-accent/40 hover:text-cyber-accent/70"
    >
      ⚙️
    </button>

    <!-- Language toggle -->
    <button
      :title="locale === 'vi' ? 'Switch to English' : 'Chuyển sang Tiếng Việt'"
      @click="toggleLang"
      class="w-9 h-6 rounded flex items-center justify-center text-xs font-mono border border-cyber-dim text-cyber-accent hover:bg-cyber-accent/10 transition-colors duration-150"
    >
      {{ t('nav.lang') }}
    </button>

    <!-- Health dot -->
    <div
      :title="healthStatus"
      :class="['w-2 h-2 rounded-full mt-1 transition-colors duration-500', isHealthy ? 'bg-cyber-accent' : 'bg-red-500']"
      :style="isHealthy ? 'box-shadow: 0 0 6px #00d4ff' : ''"
    />
  </nav>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useI18n } from 'vue-i18n'
import type { Locale } from '../i18n'

defineProps<{ activeView: string }>()
defineEmits<{ navigate: [view: string] }>()

const { t, locale } = useI18n()

const navItems = [
  { view: 'chat',  labelKey: 'nav.chat',  icon: '💬' },
  { view: 'tasks', labelKey: 'nav.tasks', icon: '📋' },
  { view: 'files', labelKey: 'nav.files', icon: '📁' },
]

const isHealthy = ref(false)
const healthStatus = ref(t('health.checking'))

function toggleLang() {
  const next: Locale = locale.value === 'vi' ? 'en' : 'vi'
  locale.value = next
  localStorage.setItem('workspace.lang', next)
}

onMounted(async () => {
  try {
    const res = await fetch('/api/health')
    const data = await res.json()
    isHealthy.value = data.status === 'ok'
    healthStatus.value = t('health.ok')
  } catch {
    healthStatus.value = t('health.error')
  }
})
</script>
```

- [ ] **Step 2: Verify trong browser**

Start dev servers (backend + frontend):
```bash
# Terminal 1
cd backend && npm run start:dev

# Terminal 2
cd frontend && npm run dev
```

Mở `http://localhost:5173`. Kiểm tra:
- Sidebar hiện 4 icons (💬 📋 📁 ⚙️) + nút `VI` nhỏ + health dot
- Tooltip của icons hiển thị tiếng Việt: "Trò chuyện", "Nhiệm vụ", v.v.
- Click `VI` → nút đổi thành `EN`, tooltip đổi thành "Chuyển sang Tiếng Việt"
- Click `EN` → nút đổi lại `VI`
- Không có console errors

- [ ] **Step 3: Commit**

```bash
cd ..
git add frontend/src/components/SidebarNav.vue
git commit -m "feat(frontend): update SidebarNav with i18n and VI/EN language toggle"
```

---

## Task 3: Update ChatPanel.vue — i18n

**Files:**
- Modify: `frontend/src/components/ChatPanel.vue`

- [ ] **Step 1: Replace toàn bộ nội dung `frontend/src/components/ChatPanel.vue`**

```vue
<template>
  <div class="flex flex-col bg-cyber-bg min-w-0">
    <!-- Header -->
    <div class="px-3 py-2 border-b border-cyber-border bg-cyber-dark flex items-center justify-between shrink-0">
      <span class="text-cyber-accent text-xs tracking-widest font-mono">◈ {{ t('chat.header') }}</span>
      <span class="text-cyber-accent/40 text-xs font-mono">{{ t('chat.mode.stub') }}</span>
    </div>

    <!-- Messages -->
    <div ref="messagesEl" class="flex-1 overflow-y-auto px-3 py-3 flex flex-col gap-3 min-h-0">
      <div v-for="(msg, i) in messages" :key="i" class="font-mono">
        <div class="text-xs mb-1" :class="roleColor(msg.role)">
          {{ rolePrefix(msg.role) }} · {{ msg.timestamp }}
        </div>
        <div
          class="text-sm leading-relaxed break-words"
          :class="{
            'text-cyber-accent/50': msg.role === 'system',
            'text-slate-300': msg.role === 'agent',
            'text-slate-100': msg.role === 'user',
          }"
        >
          {{ msg.content }}<span v-if="msg.typing" class="animate-blink text-cyber-accent ml-px">█</span>
        </div>
      </div>
    </div>

    <!-- Input -->
    <div class="px-3 py-2 border-t border-cyber-border bg-cyber-dark shrink-0">
      <form @submit.prevent="submit" class="flex items-center gap-2 border border-cyber-dim rounded px-3 py-2">
        <span class="text-cyber-accent text-sm font-mono">$</span>
        <input
          v-model="input"
          class="flex-1 bg-transparent text-slate-100 text-sm outline-none font-mono placeholder-cyber-accent/30"
          :placeholder="t('chat.placeholder')"
          :disabled="loading"
          autocomplete="off"
          spellcheck="false"
        />
        <span v-if="!loading" class="animate-blink text-cyber-accent text-sm">█</span>
        <span v-else class="text-cyber-accent/50 text-xs">{{ t('chat.loading') }}</span>
      </form>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, nextTick } from 'vue'
import { useI18n } from 'vue-i18n'

interface Message {
  role: 'user' | 'agent' | 'system'
  content: string
  timestamp: string
  typing?: boolean
}

const emit = defineEmits<{ lastMessage: [content: string] }>()
const { t } = useI18n()

const messages = ref<Message[]>([
  {
    role: 'system',
    content: t('chat.system.init'),
    timestamp: now(),
  },
])
const input = ref('')
const loading = ref(false)
const messagesEl = ref<HTMLElement | null>(null)

function now(): string {
  return new Date().toLocaleTimeString('vi-VN', { hour12: false })
}

function rolePrefix(role: string): string {
  if (role === 'user') return t('chat.user.prefix')
  if (role === 'agent') return t('chat.agent.prefix')
  return t('chat.system.prefix')
}

function roleColor(role: string): string {
  if (role === 'user') return 'text-cyber-accent/60'
  if (role === 'agent') return 'text-cyber-accent'
  return 'text-cyber-accent/40'
}

async function scrollToBottom() {
  await nextTick()
  if (messagesEl.value) {
    messagesEl.value.scrollTop = messagesEl.value.scrollHeight
  }
}

async function submit() {
  const text = input.value.trim()
  if (!text || loading.value) return
  input.value = ''
  loading.value = true

  messages.value.push({ role: 'user', content: text, timestamp: now() })
  await scrollToBottom()

  try {
    const res = await fetch('/api/agent/chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ message: text }),
    })

    if (!res.ok) {
      const err = await res.json().catch(() => ({ message: `HTTP ${res.status}` }))
      throw new Error(err.message ?? `HTTP ${res.status}`)
    }

    const data: { reply: string; timestamp: string } = await res.json()
    await typewriterAppend(data.reply)
    emit('lastMessage', data.reply)
  } catch (e) {
    const errMsg = e instanceof Error ? e.message : 'Unknown error'
    messages.value.push({
      role: 'system',
      content: `${t('chat.error.unreachable')} (${errMsg})`,
      timestamp: now(),
    })
    await scrollToBottom()
  } finally {
    loading.value = false
  }
}

async function typewriterAppend(fullText: string) {
  const msg: Message = { role: 'agent', content: '', timestamp: now(), typing: true }
  messages.value.push(msg)
  await scrollToBottom()

  for (const char of fullText) {
    msg.content += char
    await new Promise<void>(resolve => setTimeout(resolve, 18))
    await scrollToBottom()
  }
  msg.typing = false
}
</script>
```

- [ ] **Step 2: Verify trong browser**

Với dev servers đang chạy, mở `http://localhost:5173`:

1. Header ChatPanel hiển thị `◈ AGENT CHAT` + `chế độ stub` (tiếng Việt)
2. System message đầu tiên: `"Agent đã khởi động. SQLite đã kết nối. Đang ở chế độ stub."`
3. Placeholder input: `"nhập lệnh hoặc câu hỏi_"`
4. Gửi một message → prefix agent là `▶ agent`, user là `$ người dùng`
5. Click `VI` toggle → header đổi sang `"stub mode"`, placeholder đổi sang `"type a command or question_"`
6. Timestamp hiển thị 24h format (e.g., `14:30:05`)

- [ ] **Step 3: Commit**

```bash
cd ..
git add frontend/src/components/ChatPanel.vue
git commit -m "feat(frontend): update ChatPanel with i18n and vi-VN time format"
```

---

## Task 4: Update ArtifactsPanel.vue — i18n

**Files:**
- Modify: `frontend/src/components/ArtifactsPanel.vue`

- [ ] **Step 1: Replace toàn bộ nội dung `frontend/src/components/ArtifactsPanel.vue`**

```vue
<template>
  <div class="flex flex-col bg-cyber-bg min-w-0">
    <!-- Header -->
    <div class="px-3 py-2 border-b border-cyber-border bg-cyber-dark flex items-center justify-between shrink-0">
      <span class="text-cyber-accent text-xs tracking-widest font-mono">◈ {{ t('artifacts.header') }}</span>
      <span v-if="lastMessage" class="text-cyber-accent/40 text-xs font-mono">{{ t('artifacts.label.lastReply') }}</span>
    </div>

    <!-- Content -->
    <div class="flex-1 overflow-y-auto px-3 py-3 min-h-0">
      <!-- Empty state -->
      <div
        v-if="!lastMessage"
        class="flex items-center justify-center h-full text-cyber-accent/30 text-sm font-mono"
      >
        {{ t('artifacts.empty') }}
      </div>

      <!-- Code blocks -->
      <template v-else>
        <div v-for="(block, i) in codeBlocks" :key="`code-${i}`" class="mb-3">
          <div class="bg-cyber-dark border border-cyber-border rounded overflow-hidden">
            <div class="px-3 py-1 border-b border-cyber-border text-cyber-accent/50 text-xs font-mono">
              {{ block.lang || 'code' }}
            </div>
            <pre class="px-3 py-2 text-xs text-slate-300 overflow-x-auto leading-relaxed font-mono">{{ block.code }}</pre>
          </div>
        </div>

        <!-- Prose content -->
        <div
          v-if="proseHtml"
          class="text-sm text-slate-300 leading-relaxed font-mono prose-invert"
          v-html="proseHtml"
        />
      </template>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'
import { marked } from 'marked'
import DOMPurify from 'dompurify'

const props = defineProps<{ lastMessage: string }>()
const { t } = useI18n()

interface CodeBlock {
  lang: string
  code: string
}

const CODE_FENCE_RE = /```(\w*)\n([\s\S]*?)```/g

const codeBlocks = computed<CodeBlock[]>(() => {
  if (!props.lastMessage) return []
  const blocks: CodeBlock[] = []
  const re = new RegExp(CODE_FENCE_RE.source, 'g')
  let match: RegExpExecArray | null
  while ((match = re.exec(props.lastMessage)) !== null) {
    blocks.push({ lang: match[1] ?? '', code: match[2].trimEnd() })
  }
  return blocks
})

const proseHtml = computed<string>(() => {
  if (!props.lastMessage) return ''
  const prose = props.lastMessage.replace(new RegExp(CODE_FENCE_RE.source, 'g'), '').trim()
  if (!prose) return ''
  const raw = marked.parse(prose) as string
  return DOMPurify.sanitize(raw)
})
</script>
```

- [ ] **Step 2: Verify trong browser**

Mở `http://localhost:5173`:

1. Artifacts panel header: `◈ KẾT QUẢ` (tiếng Việt)
2. Empty state: `◈ Chưa có kết quả`
3. Gửi message trong chat → panel hiển thị `phản hồi cuối` badge
4. Click `VI` toggle → header đổi thành `◈ ARTIFACTS`, empty state đổi thành `◈ No artifacts yet`
5. Không có console errors

- [ ] **Step 3: Commit**

```bash
cd ..
git add frontend/src/components/ArtifactsPanel.vue
git commit -m "feat(frontend): update ArtifactsPanel with i18n"
```

---

## Task 5: End-to-End Verification

- [ ] **Step 1: Kiểm tra toàn bộ luồng ngôn ngữ**

Với dev servers đang chạy, mở `http://localhost:5173`:

| # | Hành động | Kết quả mong đợi |
|---|---|---|
| 1 | Mở app lần đầu | Toàn bộ UI = tiếng Việt |
| 2 | Xem placeholder input | `nhập lệnh hoặc câu hỏi_` |
| 3 | Gửi message | Prefix `$ người dùng`, agent reply `▶ agent` |
| 4 | Click nút `VI` sidebar | UI đổi toàn bộ sang English ngay lập tức |
| 5 | Xem placeholder input | `type a command or question_` |
| 6 | Xem Artifacts panel | Header `ARTIFACTS`, empty `◈ No artifacts yet` |
| 7 | Refresh trang | UI vẫn là English (localStorage persist) |
| 8 | Click nút `EN` | Về lại tiếng Việt |
| 9 | Refresh trang | UI vẫn là tiếng Việt |

- [ ] **Step 2: Kiểm tra console warnings**

Mở DevTools (F12) → Console tab:

Expected: Không có cảnh báo `[vue-i18n] Not found 'key'` nào. Nếu có, key đó bị thiếu trong locale file — thêm vào cả `vi.json` và `en.json`.

- [ ] **Step 3: Kiểm tra production build**

```bash
cd frontend
npm run build
```

Expected output (cuối cùng):
```
✓ built in X.XXs
dist/index.html
dist/assets/index-[hash].js
dist/assets/index-[hash].css
```

Không có TypeScript errors. Nếu có lỗi `Type 'string' is not assignable to type 'Locale'`, kiểm tra lại `locale.value = next` trong `toggleLang()` có dùng `type Locale = 'vi' | 'en'` không.

- [ ] **Step 4: Final commit**

```bash
cd ..
git add .
git commit -m "chore(frontend): phase 1 i18n complete — Vietnamese primary, EN toggle, localStorage persist"
```

---

## Verification Summary

| Check | Cách test | Kết quả mong đợi |
|---|---|---|
| Default language | Mở app | Toàn bộ UI tiếng Việt |
| Toggle to English | Click `VI` | UI đổi ngay, không reload |
| Toggle back | Click `EN` | Về tiếng Việt |
| Persist after refresh | Refresh sau toggle | Ngôn ngữ được nhớ |
| No missing keys | DevTools Console | Không có `[vue-i18n] Not found` |
| TypeScript clean | `npm run build` | Build thành công |
| Time format | Gửi message | Timestamp 24h, e.g. `14:30:05` |
